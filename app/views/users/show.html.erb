<br/>
<div class="taskwrapper jumbotron">
  <h1>S.M.A.R.T Tasks</h1>
  <p class="lead">Get Things done.</p>
  <p>
    <% if @lists.count > 0 %>
    <% else %>
      <%= link_to "Create New List", new_user_list_path(@user), class: 'btn btn-primary' %>
    <% end %>
  </p>
</div>